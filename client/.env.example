# Optional client overrides for the root `.env`. CRA inlines `REACT_APP_*` at
# build time. You can also set the same keys in Vercel. Do not put secrets here.

# IMPORTANT: do not run the dev server on port 3000. The Freebuff desktop app
# reserves that port (it reads PORT from the environment it was launched from,
# default 8787) and will probe and force-kill whatever else is listening on it
# every few minutes. The craco config clamps this automatically, but keep
# explicit overrides clear of it. Local default: PORT=3005 in client/.env.

# OSRS cache base URL (trailing slash optional). Default: /caches/
# REACT_APP_CACHE_BASE_URL=https://game.example.com/caches/

# Default WebSocket endpoint before the player picks a server.
# REACT_APP_DEFAULT_WS_URL=wss://game.example.com

# Defaults shown on the login "server" button.
# REACT_APP_DEFAULT_SERVER_ADDRESS=game.example.com
# REACT_APP_DEFAULT_SERVER_NAME=My Server
# REACT_APP_DEFAULT_SERVER_SECURE=true

# Optional JSON array baked into the client (skips fetching /servers.json).
# REACT_APP_SERVERS_JSON=[{"name":"My Server","address":"game.example.com","secure":true,"maxPlayers":2047}]

# Optional remote server-list URL. Default: same-origin /servers.json
# REACT_APP_SERVER_LIST_URL=/servers.json

# Optional public WebRTC relay directory. Localhost defaults to ws://127.0.0.1:8787.
# REACT_APP_WEBRTC_SIGNAL_URL=wss://relay.example.com
# REACT_APP_WEBRTC_ICE_SERVERS=[{"urls":"stun:relay.example.com:3478"}]

FAST_REFRESH=false
